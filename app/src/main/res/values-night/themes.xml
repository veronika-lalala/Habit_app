<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Myappp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green</item>
        <!-- Цвета для темного режима -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:textColorTertiary">@color/white</item>
        <item name="android:textColorHint">@color/white</item>

        <!-- Специфические настройки для MaterialCalendarView -->
        <item name="mcv_weekDayTextAppearance">@color/white</item>
        <item name="mcv_headerTextAppearance">@color/white</item>
        <item name="mcv_dateTextAppearance">@color/white</item>
    </style>
</resources>
